"""
image_tools/ocr_tools.py -- PLACEHOLDER, not yet active.

Will hold: everything currently in ocr_date/ocr_date.py (find_date_in_corners()
and its helpers), moved here as-is once this migration reaches that step.

The existing ocr_date/ folder is UNTOUCHED and still fully working --
this is only a destination for a later move, not a replacement yet.
test_functions/test_ocr_date.py still imports from ocr_date/, not here,
until that move happens and gets retested.
"""
